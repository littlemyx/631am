/* eslint-disable @next/next/no-html-link-for-pages */
import React from "react";

import TextLink from "@/components/TextLink";

const CV = () => {
  return <TextLink href="/cv" text="CV" />;
};

export default CV;
